# Collection Thumbnail Builder

A self-service tool for composing MyHeritage historical-collection thumbnails and
downloading them as transparent PNGs. Open the live tool:

**https://anyaborokhovich-rgb.github.io/collection-thumbnail-builder/**

Everything (code + images) is embedded in `index.html`; it runs entirely in the browser, offline.
